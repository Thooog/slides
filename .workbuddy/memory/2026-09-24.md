# 2026-09-24

## T004 Executor Revision 5（媒体读取与基础浏览闭环）

- 用户决策：恢复最小重选入口（解除 Revision 4 的 BLOCKED）。
- 修复：`Permissions.currentScope()` 改回官方推荐顺序（READ_MEDIA_IMAGES/VIDEO 优先，user-selected 兜底），修复 MIUI 完整授权（同时授予 VISUAL_USER_SELECTED）被误判 SELECTED。
- 新增：HomeScreen `AccessBanner`（scope≠FULL 单行横幅；SELECTED→photo picker 重新选择 PickMultipleVisualMedia/ImageAndVideo；单类型→去设置）。
- 构建：BUILD SUCCESSFUL；APK SHA256 4633A052AF1E7644C4B85DED1013A105B8F540342B3D90BCECB01457C8645A19；Xiaomi 13 安装启动无 FATAL。
- 已验证：完整授权下横幅正确隐藏（uiautomator dump + 截图）；部分授权分支 R5-1~R5-7 列入 manual_test_checklist.md 待用户手动复核。
- 记录：R004.md 追加 Executor Revision 5，Reviewer Verdict PENDING_REVIEW。
- 注意：本机 Bash 环境 PATH 损坏（grep/head/tr 不可用），shell 命令优先用 PowerShell + Out-File 落盘再 Read 的模式。

## T005 修复与真机验收（只读相册浏览闭环）

- 修复 4 类源码缺口：① MediaRepository 查询异常不再吞成成功空列表（双失败→failure）；② HomeContainer 详情快照随访问范围核对（LaunchedEffect(state.allItems) 失效详情）；③ pickImagesLauncher 回调按 uris.isNotEmpty() 决定是否刷新（取消不制造授权成功）；④ 手势：详情双击移除缩放（PRD 双击留给收藏）、缩放改 relative span 增量、网格 detectTwoFingerPinch 用 rememberUpdatedState 防旧列数。
- 构建 BUILD SUCCESSFUL，APK SHA256 4FA6AEB9D650C5CB712DA754706F52A82199DFD805DB89D942CBF689384F59EB。
- 真机（小米13/Android15）用户反馈：A9/B1-B6/C1-C3/D1-D3/F1-F4 PASS；D4/E1 UNVERIFIED（无法构造失效媒体）；A1-A8 及 E2 因 MIUI 不支持分级授权标 NOT_APPLICABLE（用户确认）。
- 关键设备限制：小米13系统设置仅二元「允许/拒绝」，Android14 部分照片访问需 photo picker 触发，SELECTED/单类型状态难以真机构造。
- 记录：evidence/T005/manual_test_checklist.md（清单）+ reviews/R005.md（Executor Record，READY_FOR_REVIEW，AC2 分级授权分支 NOT_APPLICABLE 待 Reviewer 判断口径）。
- 用户偏好：真机测试项需整理成可勾选 list 交给用户手动执行；不接受 pm 命令切换权限。

## T006 收藏真机反馈与修复（进行中）

- 用户真机反馈：A2/A3/A5/B1-B3/C1-C4/D1/F1-F3 全 PASS；A4（详情图片双击）、D2（详情视频双击）FAIL「双击无变化」；E1/E2 无法判断。
- 用户决策（AskUserQuestion）：**移除网格双击收藏**（A1 改「网格双击无功能，仅详情页双击收藏」）——注意这偏离 PRD §6.1「网格双击收藏」规格，需在 R006 记录为规格偏离。
- 修复：① DetailScreen 图片双击失效根因——`lastTapTime` 是 awaitEachGesture 块内局部变量，每次手势结束被重置，双击永远无法识别；已提升到 pointerInput 块作用域，并用 SystemClock.uptimeMillis() 取抬手时刻。② 视频双击未实现——VideoPlayerView 加 onDoubleTap + detectTapGestures 覆盖层（onTap 空实现，单击仍交 PlayerView）。
- HomeScreen：移除网格双击，MediaCell 改简单 clickable；删 onDoubleClick 参数、lastTapMs 去抖、detectTapGestures/mutableLongStateOf import；MediaGrid 及 HomeScreen 移除已无用的 onToggleFavorite 参数（DetailScreen 仍保留 vm::toggleFavorite）。
- 待办：构建 + 安装 + 更新 manual_test_checklist.md（A1 改详情双击、A4/D2 重测）+ 创建 R006.md。

## T006 追加功能：视频详情左右滑动切换媒体

- 用户请求：视频详情页播放时左右滑动也能切换媒体（此前仅底栏 ‹/› 按钮）。
- 实现：`VideoPlayerView` 增加 `onSwipeNext/onSwipePrev` 参数；手势层从 `detectTapGestures` 改为自研 `awaitEachGesture`，同时处理「双击收藏 + 单指横向滑动切换（位移≥120px）+ 轻点不消费交播放器」。多指不处理避免误判；移除 detectTapGestures import。
- 构建 BUILD SUCCESSFUL（37 tasks executed）；APK SHA256 0D163BA68FB1C410DB8F37D94071931B26E23BE7AC0038ADB068BFD72E14507D；安装 Success；启动 PID=23084 无 FATAL。
- 证据：apk_summary_round3.txt / install_round3.txt / start_round3.txt / fatal_check_round3.txt。
- 待办：用户真机验证视频左右滑动切换（新增测试项）。
